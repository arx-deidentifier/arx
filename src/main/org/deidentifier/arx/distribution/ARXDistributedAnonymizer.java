package org.deidentifier.arx.distribution;

import java.io.IOException;

import org.deidentifier.arx.ARXConfiguration;
import org.deidentifier.arx.Data;

public class ARXDistributedAnonymizer {
    
    /**
     * Performs data anonymization.
     *
     * @param data The data
     * @param config The privacy config
     * @return ARXResult
     * @throws IOException
     */
    public ARXDistributedResult anonymize(final Data data, ARXConfiguration config) throws IOException {
        return null;
    }
}
